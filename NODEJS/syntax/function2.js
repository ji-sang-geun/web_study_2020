console.log(Math.round(1.6));// 2 ->
console.log(Math.round(1.4));// 1

function sum(first, second){ // 함수는 융통성이 떨어짐
  // parameter : argument(인자)들을 받아서 함수로 넣어주는 매개체.
  return first + second;
}

console.log(sum(2,4)); // argument : 입력값
